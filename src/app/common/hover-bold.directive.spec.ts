import { HoverBoldDirective } from './hover-bold.directive';

describe('HoverBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverBoldDirective();
    expect(directive).toBeTruthy();
  });
});
